interface:
  display_name: "Trigger Release Build"
  short_description: "Safely publish or resume from origin/main"
  default_prompt: "Use $trigger-release-build to publish or resume the requested release version from the current origin/main."
policy:
  allow_implicit_invocation: false
