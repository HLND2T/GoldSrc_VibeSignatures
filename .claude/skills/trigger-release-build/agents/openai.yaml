interface:
  display_name: "Trigger Release Build"
  short_description: "Publish from origin/main with rebuild or tracked artifacts"
  default_prompt: "Use $trigger-release-build to publish or resume the requested release from origin/main, asking for rebuild or build-free when unspecified."
policy:
  allow_implicit_invocation: false
