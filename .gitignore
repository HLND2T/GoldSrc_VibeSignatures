.env
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Downloaded depots, binaries, IDA databases, and local release candidates.
depots/
.candidates/
.gamedata-candidates/

# Generated production outputs are promoted deliberately.
gamesymbols/*.yaml
gamedata/*/

# Basic Memory knowledge base (memory.db, config, logs stay local)
memory/*.db*
memory/config.json
